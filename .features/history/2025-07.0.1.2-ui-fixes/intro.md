# Gomoku 0.1.2 — a round of polish

A small, quick release: a batch of fixes and tidy-ups to how the game looks and
behaves in the terminal, plus a fix to the machinery that publishes a tagged
version. Nothing dramatic — the kind of cleanup pass that makes the previous
release feel finished.
