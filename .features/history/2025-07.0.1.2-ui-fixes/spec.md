# Spec — 0.1.2 UI fixes and release tooling

**Span:** `6610c3e` → `6e4df5e` (4 commits, 2025-07-08)
**Version:** `0.1.1` → `0.1.2`

## Theme

A short polish release: assorted UI fixes and a correction to the tag-publishing
flow.

## What was built

- **"Many fixes and UI updates"** — a consolidated round of terminal-UI
  corrections and display adjustments.
- **Release tooling** — a "Force tag push" fix to the process that publishes the
  version tag (the origin of the later `vv0.1.2` typo anomaly noted in the index
  README).
- Merged via PR #3 (`kig/version-0.1`).

## Oracle: version-bump assessment

A **patch** bump (`0.1.1` → `0.1.2`). Pure fixes and release-process plumbing,
no new capability and no compatibility impact.
